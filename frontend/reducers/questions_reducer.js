// import actions
import { merge } from 'lodash';

const questionsReducer = (state = {}, action) => {
  Object.freeze(state);
  let newState;

  switch (action.typ) {
    default:
      return state;
  }
};

export default questionsReducer;